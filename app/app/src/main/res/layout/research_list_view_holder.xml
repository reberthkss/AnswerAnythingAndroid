<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="title"
            type="String" />
        <variable
            name="subtitle"
            type="String" />
        <variable
            name="description"
            type="String" />
    </data>

    <LinearLayout
        android:id="@+id/research_view_holder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingLeft="25dp"
        android:paddingTop="10dp"
        android:paddingRight="25dp"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/researchTitle"
            style="@android:style/Widget.DeviceDefault.TextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{title}"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1"
            android:textColor="#000000"
            android:textSize="24sp"
            android:textStyle="bold"
            tools:text="Bairros mais ricos" />

        <TextView
            android:id="@+id/researchSubtitle"
            style="@android:style/Widget.DeviceDefault.TextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{subtitle}"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1"
            android:textColor="#0B0A0A"
            android:textSize="18sp"
            tools:text="Os bairros mais perigosos de jacareí" />

        <TextView
            android:id="@+id/researchDescription"
            style="@android:style/Widget.DeviceDefault.TextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{description}"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1"
            android:textColor="#8E8E8E"
            android:textSize="18sp"
            tools:text="O objetivo é conhecer os bairros mais perigosos de jacareí" />
    </LinearLayout>
</layout>